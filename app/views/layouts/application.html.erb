<!DOCTYPE html>
<html>
  <head>
    <title>HurbanFood</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render "shared/navbar" unless @hide_nav_bar %>
    <%= render "shared/flashes" %>
    <%= yield %>
    <%= render "shared/footer"  unless @hide_footer || controller_name == 'registrations' || controller_name == 'sessions' || controller_name == 'passwords' %>
  </body>
</html>
